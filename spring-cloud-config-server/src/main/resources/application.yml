server.port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nkchauhan003/test-config
          default-label: main
          clone-on-start: true
  security:
    user:
      name: admin
      password: admin123
